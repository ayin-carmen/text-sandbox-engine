[project]
name = "text-sandbox-engine"
version = "0.1.0"
description = "A modular, command-driven text sandbox engine prototype."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
